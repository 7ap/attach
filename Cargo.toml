[package]
name = "attach"
version = "0.0.0"
edition = "2021"
description = "Cross platform library injector written in Rust."
license = "MIT"

[dependencies]
clap = { version = "4.0.9", features = ["derive"] }

[target.'cfg(target_os = "linux")'.dependencies]
nix = "0.25.0"
